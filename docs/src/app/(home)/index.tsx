import { Home } from "../../shell/home";

// The marketing landing, rendered universally on
// iOS / Android / web (hero + orbit, principles, get-started, atomic levels, CTA, footer).
export default Home;
