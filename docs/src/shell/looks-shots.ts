// Native fallback for the landing comparison rotator's screenshot map. The real map
// lives in looks-shots.web.ts (GENERATED by scripts/capture-looks.ts) and resolves only
// on web, so the ~3MB of full device-screen captures never ship in the native app
// bundles and the landing section renders web-only (on a device you ARE the platform).
// Keep this empty; regenerate the web map with `bun scripts/capture-looks.ts`.
export const LOOKS_SHOTS: Record<string, { ios: number; android: number; web: number }> = {};

export const LOOKS_ASPECT = 1206 / 2622;
