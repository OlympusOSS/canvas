// Moved to docs-core/data so both the Vite web docs and the native Expo docs share
// one canonical source. This shim keeps the web app's `@/data/components` imports working.
export * from "../../../docs-core/data/components";
