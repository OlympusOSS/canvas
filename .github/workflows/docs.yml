name: Deploy docs to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docs site not yet configured for Canvas v3."
