{
  "note": "Generated by tools/handoff-parity/extract.ts. The design hand-off's component prop contracts, snapshotted so scripts/check-handoff-parity.ts can run without the export present.",
  "components": {
    "Accordion": {
      "tier": "molecules",
      "props": {
        "items": {
          "type": "Array<{ title: string; description?: string; content: React.ReactNode }>",
          "required": true,
          "doc": "The rows."
        },
        "multiple": {
          "type": "boolean",
          "required": false,
          "doc": "Allow several open at once."
        },
        "defaultOpen": {
          "type": "number[]",
          "required": false,
          "doc": "Initially open indices."
        },
        "card": {
          "type": "boolean",
          "required": false,
          "doc": "Card surface: wraps the group in an outlined card container with inset headers and content (a no-op on iOS, where the default already renders as the inset-grouped card)."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "ActionPanel": {
      "tier": "molecules",
      "props": {
        "title": {
          "type": "string",
          "required": true,
          "doc": "Section title."
        },
        "description": {
          "type": "string",
          "required": false,
          "doc": "Explanation."
        },
        "action": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "The control."
        },
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Extra body content."
        },
        "stacked": {
          "type": "boolean",
          "required": false,
          "doc": "Drop the action below the copy instead of pinning it to the trailing edge."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Danger zone: paints the headline red; omit for the neutral tone."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "ActionSheet": {
      "tier": "organisms",
      "props": {
        "title": {
          "type": "string",
          "required": false,
          "doc": "Muted sheet caption."
        },
        "message": {
          "type": "string",
          "required": false,
          "doc": "Optional header message under the title."
        },
        "items": {
          "type": "Array<{ label: string; destructive?: boolean; onSelect?: () => void }>",
          "required": true,
          "doc": "The actions."
        },
        "cancelLabel": {
          "type": "string",
          "required": false,
          "doc": "Cancel row label."
        },
        "open": {
          "type": "boolean",
          "required": false,
          "doc": "Visibility."
        },
        "onClose": {
          "type": "() => void",
          "required": false,
          "doc": "Dismiss handler."
        },
        "glass": {
          "type": "boolean",
          "required": false,
          "doc": "Glass material."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Alert": {
      "tier": "molecules",
      "props": {
        "title": {
          "type": "string",
          "required": false,
          "doc": "Headline."
        },
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Body copy."
        },
        "icon": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Leading glyph."
        },
        "actions": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Action row below the body."
        },
        "onDismiss": {
          "type": "() => void",
          "required": false,
          "doc": "Show a dismiss control."
        },
        "info": {
          "type": "boolean",
          "required": false,
          "doc": "Blue informational tone."
        },
        "success": {
          "type": "boolean",
          "required": false,
          "doc": "Green tone."
        },
        "warning": {
          "type": "boolean",
          "required": false,
          "doc": "Amber tone."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Red tone."
        },
        "narrow": {
          "type": "boolean",
          "required": false,
          "doc": "320px measure — a sidebar or phone column."
        },
        "wide": {
          "type": "boolean",
          "required": false,
          "doc": "640px measure — a page-width banner."
        },
        "block": {
          "type": "boolean",
          "required": false,
          "doc": "Fill the container with no cap."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "AlertDialog": {
      "tier": "molecules",
      "props": {
        "title": {
          "type": "string",
          "required": true,
          "doc": "What is about to happen."
        },
        "description": {
          "type": "string",
          "required": true,
          "doc": "The consequence."
        },
        "confirmLabel": {
          "type": "string",
          "required": false,
          "doc": "Confirm label (default \"Delete\")."
        },
        "cancelLabel": {
          "type": "string",
          "required": false,
          "doc": "Cancel label."
        },
        "onConfirm": {
          "type": "() => void",
          "required": false,
          "doc": "Confirm handler."
        },
        "onCancel": {
          "type": "() => void",
          "required": false,
          "doc": "Cancel handler."
        },
        "open": {
          "type": "boolean",
          "required": false,
          "doc": "Visibility."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Destructive confirm fill (default true)."
        },
        "confirmField": {
          "type": "string",
          "required": false,
          "doc": "Prompt for a typed confirmation, e.g. \"Type DELETE to confirm\"."
        },
        "dismissible": {
          "type": "boolean",
          "required": false,
          "doc": "Scrim press and Escape cancel (default true). Set false when the decision must be answered."
        },
        "width": {
          "type": "number",
          "required": false,
          "doc": "Card width. Default 448."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "AreaChart": {
      "tier": "charts",
      "props": {
        "series": {
          "type": "Array<{ label: string; data: number[]; color?: string }>",
          "required": true,
          "doc": "The series."
        },
        "title": {
          "type": "string",
          "required": false,
          "doc": "Chart title."
        },
        "xLabels": {
          "type": "string[]",
          "required": false,
          "doc": "Axis labels."
        },
        "height": {
          "type": "number",
          "required": false,
          "doc": "Plot height."
        },
        "bare": {
          "type": "boolean",
          "required": false,
          "doc": "Drop the card surface."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Autocomplete": {
      "tier": "atoms",
      "props": {
        "options": {
          "type": "Array<{ value: string; label: string } | string>",
          "required": true,
          "doc": "The candidates."
        },
        "label": {
          "type": "string",
          "required": false,
          "doc": "Field title. Web and iOS render it above the field; Android floats the Material 3 in-container label (resting inside the filled box, floating up to body-small once the list opens or a value fills the field)."
        },
        "required": {
          "type": "boolean",
          "required": false,
          "doc": "Marks the label with a destructive asterisk (hidden from assistive tech)."
        },
        "helper": {
          "type": "string",
          "required": false,
          "doc": "Optional muted helper line rendered below the option list."
        },
        "placeholder": {
          "type": "string",
          "required": false,
          "doc": "Placeholder."
        },
        "value": {
          "type": "string",
          "required": false,
          "doc": "Initial query."
        },
        "open": {
          "type": "boolean",
          "required": false,
          "doc": "Start with the list open."
        },
        "onSelect": {
          "type": "(value: string) => void",
          "required": false,
          "doc": "Selection handler."
        },
        "block": {
          "type": "boolean",
          "required": false,
          "doc": "Fill the container instead of the standard width (default 320px). Wins over narrow."
        },
        "narrow": {
          "type": "boolean",
          "required": false,
          "doc": "Compact field: 240px instead of the standard 320px, for toolbars and short values."
        },
        "disabled": {
          "type": "boolean",
          "required": false,
          "doc": "Dims the control and blocks interaction."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Avatar": {
      "tier": "atoms",
      "props": {
        "src": {
          "type": "string",
          "required": false,
          "doc": "Image URL."
        },
        "name": {
          "type": "string",
          "required": false,
          "doc": "Full name; initials and the stable per-name fill colour are derived from it."
        },
        "initials": {
          "type": "string",
          "required": false,
          "doc": "Explicit initials, used verbatim (the fill colour still keys off `name` when present)."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "24px."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "40px."
        },
        "size": {
          "type": "number",
          "required": false,
          "doc": "Explicit diameter."
        },
        "rounded": {
          "type": "boolean",
          "required": false,
          "doc": "Rounded square (6px) instead of a circle."
        },
        "ring": {
          "type": "boolean",
          "required": false,
          "doc": "Background-colored ring, for stacks."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "AvatarMenu": {
      "tier": "atoms",
      "props": {
        "name": {
          "type": "string",
          "required": false,
          "doc": "The person's name: shown in the pill and as the menu's header."
        },
        "email": {
          "type": "string",
          "required": false,
          "doc": "Secondary line (usually the email)."
        },
        "src": {
          "type": "string",
          "required": false,
          "doc": "Photo URL; falls back to initials."
        },
        "initials": {
          "type": "string",
          "required": false
        },
        "items": {
          "type": "AvatarMenuItem[]",
          "required": false
        },
        "align": {
          "type": "\"start\" | \"end\"",
          "required": false,
          "doc": "Which edge the menu aligns to. Default \"end\"."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "Avatar and chevron only — no name block."
        },
        "disabled": {
          "type": "boolean",
          "required": false
        },
        "onSelect": {
          "type": "(label: string, item: AvatarMenuItem) => void",
          "required": false
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false
        }
      }
    },
    "Badge": {
      "tier": "atoms",
      "props": {
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "The label."
        },
        "status": {
          "type": "boolean",
          "required": false,
          "doc": "Status family: a fully rounded pill with a leading dot."
        },
        "default": {
          "type": "boolean",
          "required": false,
          "doc": "Brand-filled metadata pill."
        },
        "secondary": {
          "type": "boolean",
          "required": false,
          "doc": "Muted metadata pill (the default)."
        },
        "outline": {
          "type": "boolean",
          "required": false,
          "doc": "Hairline metadata pill."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Destructive metadata pill."
        },
        "mono": {
          "type": "boolean",
          "required": false,
          "doc": "Monospace label for tokens and event names."
        },
        "success": {
          "type": "boolean",
          "required": false,
          "doc": "Status: green."
        },
        "warning": {
          "type": "boolean",
          "required": false,
          "doc": "Status: amber."
        },
        "error": {
          "type": "boolean",
          "required": false,
          "doc": "Status: red."
        },
        "info": {
          "type": "boolean",
          "required": false,
          "doc": "Status: blue."
        },
        "neutral": {
          "type": "boolean",
          "required": false,
          "doc": "Status: muted."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Board": {
      "tier": "organisms",
      "props": {
        "columns": {
          "type": "BoardColumn[]",
          "required": true,
          "doc": "The columns (lanes), left to right."
        },
        "items": {
          "type": "BoardItem[]",
          "required": false,
          "doc": "CONTROLLED list; order within a column is array order filtered by `columnId`. The board never mutates it: apply each `onMove` yourself. Omit it (optionally passing `defaultItems`) for uncontrolled use."
        },
        "defaultItems": {
          "type": "BoardItem[]",
          "required": false,
          "doc": "Initial list for UNCONTROLLED use; Board then applies each move internally."
        },
        "onMove": {
          "type": "(move: BoardMove) => void",
          "required": false,
          "doc": "Fired on every completed drop with the computed move (source, target, index, neighbors)."
        },
        "onPressItem": {
          "type": "(item: BoardItem) => void",
          "required": false,
          "doc": "Makes each card body pressable and reports the pressed item."
        },
        "columnWidth": {
          "type": "number",
          "required": false,
          "doc": "Column width in px (default 300)."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "Density axis: tighter lane padding and card gaps."
        },
        "emptyLabel": {
          "type": "string",
          "required": false,
          "doc": "Message shown in an empty column (default \"No items\")."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Breadcrumb": {
      "tier": "atoms",
      "props": {
        "items": {
          "type": "Array<{ label: string; href?: string } | string>",
          "required": true,
          "doc": "The crumbs, root first."
        },
        "chevron": {
          "type": "boolean",
          "required": false,
          "doc": "The reading-direction chevron divider (the default)."
        },
        "slash": {
          "type": "boolean",
          "required": false,
          "doc": "A forward-slash divider."
        },
        "dot": {
          "type": "boolean",
          "required": false,
          "doc": "A centered middot divider."
        },
        "separator": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Legacy custom divider node; the boolean axis above is the canonical API and wins when set."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Button": {
      "tier": "atoms",
      "props": {
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "The label."
        },
        "primary": {
          "type": "boolean",
          "required": false,
          "doc": "Brand filled (the default look)."
        },
        "secondary": {
          "type": "boolean",
          "required": false,
          "doc": "Muted filled."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Destructive filled."
        },
        "outline": {
          "type": "boolean",
          "required": false,
          "doc": "Hairline outline, transparent fill."
        },
        "ghost": {
          "type": "boolean",
          "required": false,
          "doc": "No fill, no border."
        },
        "link": {
          "type": "boolean",
          "required": false,
          "doc": "Underlined brand text."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "32px control, 12/16 label."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "48px control, 16/24 label."
        },
        "icon": {
          "type": "boolean",
          "required": false,
          "doc": "Square icon-only button (32/40/48)."
        },
        "block": {
          "type": "boolean",
          "required": false,
          "doc": "Fill the container width."
        },
        "loading": {
          "type": "boolean",
          "required": false,
          "doc": "Show a spinner and dim the control."
        },
        "disabled": {
          "type": "boolean",
          "required": false,
          "doc": "Dim to 0.5 and stop pointer events."
        },
        "iconLeft": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Glyph before the label."
        },
        "iconRight": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Glyph after the label."
        },
        "onClick": {
          "type": "() => void",
          "required": false,
          "doc": "Press handler."
        },
        "type": {
          "type": "\"button\" | \"submit\"",
          "required": false,
          "doc": "Form behaviour."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "ButtonGroup": {
      "tier": "atoms",
      "props": {
        "items": {
          "type": "Array<{ value?: string; label: string } | string>",
          "required": true,
          "doc": "The segments."
        },
        "value": {
          "type": "string",
          "required": false,
          "doc": "Selected segment value."
        },
        "onChange": {
          "type": "(value: string) => void",
          "required": false,
          "doc": "Selection handler."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "Compact segments."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "Taller segments."
        },
        "block": {
          "type": "boolean",
          "required": false,
          "doc": "Stretch to the container width."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Calendar": {
      "tier": "organisms",
      "props": {
        "month": {
          "type": "string",
          "required": false,
          "doc": "Header label."
        },
        "start": {
          "type": "number",
          "required": false,
          "doc": "Weekday offset of the 1st (0 = Monday)."
        },
        "days": {
          "type": "number",
          "required": false,
          "doc": "Days in the month."
        },
        "today": {
          "type": "number",
          "required": false,
          "doc": "Day to ring."
        },
        "selected": {
          "type": "number",
          "required": false,
          "doc": "Day to fill in single-date mode."
        },
        "events": {
          "type": "number[]",
          "required": false,
          "doc": "Days carrying an event dot."
        },
        "range": {
          "type": "boolean",
          "required": false,
          "doc": "Switch to a range picker: click start, click end, hover previews the span."
        },
        "rangeStart": {
          "type": "number | null",
          "required": false,
          "doc": "Controlled range start. Omit both to let the calendar hold its own."
        },
        "rangeEnd": {
          "type": "number | null",
          "required": false,
          "doc": "Controlled range end."
        },
        "onRangeChange": {
          "type": "(range: DateRange) => void",
          "required": false,
          "doc": "Fires whenever the range changes, half-open included."
        },
        "onSelect": {
          "type": "(day: number) => void",
          "required": false,
          "doc": "Day press handler — fires in both modes."
        },
        "onPrev": {
          "type": "() => void",
          "required": false,
          "doc": "Previous-month chevron."
        },
        "onNext": {
          "type": "() => void",
          "required": false,
          "doc": "Next-month chevron."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "Tighter 32px cells."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "CandlestickChart": {
      "tier": "charts",
      "props": {
        "candles": {
          "type": "Candle[]",
          "required": true
        },
        "title": {
          "type": "string",
          "required": false
        },
        "subtitle": {
          "type": "string",
          "required": false,
          "doc": "Right-aligned caption in the header strip."
        },
        "height": {
          "type": "number",
          "required": false,
          "doc": "Total plot height in px, candles and volume together."
        },
        "xLabels": {
          "type": "string[]",
          "required": false
        },
        "ma": {
          "type": "number[]",
          "required": false,
          "doc": "Moving-average periods to overlay, e.g. `[7, 21]`."
        },
        "volume": {
          "type": "boolean",
          "required": false,
          "doc": "Volume histogram below the candles. Default true."
        },
        "priceAxis": {
          "type": "boolean",
          "required": false,
          "doc": "Right-hand price gutter. Default true."
        },
        "lastPrice": {
          "type": "boolean",
          "required": false,
          "doc": "Dashed last-price line and tag. Default true."
        },
        "crosshair": {
          "type": "boolean",
          "required": false,
          "doc": "Hover crosshair and header read-out. Default true."
        },
        "currency": {
          "type": "string",
          "required": false,
          "doc": "Prefix on the last-price tag. Default \"$\"."
        },
        "decimals": {
          "type": "number",
          "required": false,
          "doc": "Decimal places on every price. Default 2."
        },
        "compact": {
          "type": "boolean",
          "required": false
        },
        "bare": {
          "type": "boolean",
          "required": false,
          "doc": "Drop the card surface."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        },
        "children": {
          "type": "React.ReactNode",
          "required": false
        }
      }
    },
    "Card": {
      "tier": "molecules",
      "props": {
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Card content, usually CardHeader / CardContent / CardFooter; stays the body and wins over `body` when both are passed."
        },
        "title": {
          "type": "string",
          "required": false,
          "doc": "The card's heading, rendered in a self-padding header section."
        },
        "icon": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "A glyph rendered before the title, for a section panel that names itself with an icon."
        },
        "actions": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Trailing header content, typically a button; sits opposite the title so the header reads as one row."
        },
        "description": {
          "type": "string",
          "required": false,
          "doc": "The muted supporting line beneath the title."
        },
        "body": {
          "type": "string",
          "required": false,
          "doc": "Body copy for the children-less, data-driven card; ignored when children are passed."
        },
        "footer": {
          "type": "string",
          "required": false,
          "doc": "Muted summary line rendered in a footer section beneath a separator."
        },
        "padded": {
          "type": "boolean",
          "required": false,
          "doc": "Explicit standard 24px inset (the default); inert on a sectioned card, whose sections pad themselves."
        },
        "flush": {
          "type": "boolean",
          "required": false,
          "doc": "No inset (for media or list children); inert on a sectioned card."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "16px inset."
        },
        "comfortable": {
          "type": "boolean",
          "required": false,
          "doc": "32px inset."
        },
        "raised": {
          "type": "boolean",
          "required": false,
          "doc": "shadow-md."
        },
        "flat": {
          "type": "boolean",
          "required": false,
          "doc": "No shadow."
        },
        "selected": {
          "type": "boolean",
          "required": false,
          "doc": "Brand-colored hairline."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Carousel": {
      "tier": "organisms",
      "props": {
        "slides": {
          "type": "React.ReactNode[]",
          "required": true,
          "doc": "The slides."
        },
        "height": {
          "type": "number",
          "required": false,
          "doc": "Stage height."
        },
        "showArrows": {
          "type": "boolean",
          "required": false,
          "doc": "Force the edge arrows on or off. Defaults to the platform skin."
        },
        "showDots": {
          "type": "boolean",
          "required": false,
          "doc": "Force the dot strip on or off. Defaults to the platform skin."
        },
        "swipe": {
          "type": "boolean",
          "required": false,
          "doc": "Drag/swipe to move between slides. Default true."
        },
        "loop": {
          "type": "boolean",
          "required": false,
          "doc": "Wrap past the last slide. Default true; false rubber-bands at the edges."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Chart": {
      "tier": "charts",
      "props": {
        "data": {
          "type": "Array<{ label: string; value: number; color?: string }>",
          "required": false,
          "doc": "The categories (single-series mode)."
        },
        "labels": {
          "type": "string[]",
          "required": false,
          "doc": "Category labels along the x axis (grouped mode, with `series`)."
        },
        "series": {
          "type": "Array<{ label: string; values: number[] }>",
          "required": false,
          "doc": "Multiple series rendered as clustered columns per category, colored by the chart-1..8 tokens in fixed series order."
        },
        "title": {
          "type": "string",
          "required": false,
          "doc": "Chart title (600 weight, 16/24; 14/20 compact)."
        },
        "horizontal": {
          "type": "boolean",
          "required": false,
          "doc": "Rows with a 64px leading label instead of columns. Single-series only."
        },
        "tone": {
          "type": "\"primary\" | \"success\" | \"destructive\"",
          "required": false,
          "doc": "Bar fill: the brand token, green-500, or red-500. Single-series only; grouped series use the chart tokens."
        },
        "height": {
          "type": "number",
          "required": false,
          "doc": "Plot height for the vertical layout."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "16px surface padding and the smaller title."
        },
        "hideLegend": {
          "type": "boolean",
          "required": false,
          "doc": "Hide the series legend (grouped mode shows one by default)."
        },
        "selected": {
          "type": "number | null",
          "required": false,
          "doc": "Press-to-inspect: the selected category index (controlled). Pass null for none. Vertical charts only."
        },
        "defaultSelected": {
          "type": "number",
          "required": false,
          "doc": "Press-to-inspect: the initially selected category (uncontrolled)."
        },
        "onSelect": {
          "type": "(index: number | null) => void",
          "required": false,
          "doc": "Fired when a press selects a category (or clears it with null)."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "ChartFrame": {
      "tier": "charts",
      "props": {
        "title": {
          "type": "string",
          "required": false,
          "doc": "Chart title."
        },
        "series": {
          "type": "Array<{ label: string; color?: string }>",
          "required": false,
          "doc": "Legend entries, rendered as the kit's round-dot legend below the plot."
        },
        "xLabels": {
          "type": "string[]",
          "required": false,
          "doc": "Axis labels below the plot."
        },
        "height": {
          "type": "number",
          "required": false,
          "doc": "Plot height."
        },
        "yDomain": {
          "type": "[number, number]",
          "required": false
        },
        "formatValue": {
          "type": "(v: number) => string",
          "required": false,
          "doc": "Tick/label formatter (defaults to compact \"1.2k\" notation)."
        },
        "yTicks": {
          "type": "number",
          "required": false,
          "doc": "Gridline count (no `yDomain`), or the target tick count for the niced axis."
        },
        "insetLeft": {
          "type": "number",
          "required": false,
          "doc": "Baseline and label-row left inset (px), matching the plot's axis gutter."
        },
        "insetRight": {
          "type": "number",
          "required": false,
          "doc": "Baseline and label-row right inset (px), matching the plot's axis gutter."
        },
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "The plot."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "Tighter padding and title."
        },
        "bare": {
          "type": "boolean",
          "required": false,
          "doc": "Drop the card surface (embed in an existing card)."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Checkbox": {
      "tier": "atoms",
      "props": {
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "The label title."
        },
        "description": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Muted secondary line."
        },
        "checked": {
          "type": "boolean",
          "required": false,
          "doc": "Checked state."
        },
        "indeterminate": {
          "type": "boolean",
          "required": false,
          "doc": "Mixed state."
        },
        "onChange": {
          "type": "(checked: boolean) => void",
          "required": false,
          "doc": "Toggle handler."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "14px box."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "20px box."
        },
        "disabled": {
          "type": "boolean",
          "required": false,
          "doc": "Dim and disable."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Chip": {
      "tier": "atoms",
      "props": {
        "children": {
          "type": "React.ReactNode",
          "required": true,
          "doc": "The tag text."
        },
        "onRemove": {
          "type": "() => void",
          "required": false,
          "doc": "Show a remove affordance: the lucide `x` glyph at the platform size (12px web/iOS, 18dp M3)."
        },
        "selected": {
          "type": "boolean",
          "required": false,
          "doc": "Selected (brand tint). On Android it renders the M3 selected filter-chip anatomy: a leading 18dp checkmark and the outline drops."
        },
        "primary": {
          "type": "boolean",
          "required": false,
          "doc": "Brand tint."
        },
        "success": {
          "type": "boolean",
          "required": false,
          "doc": "Success tint."
        },
        "warning": {
          "type": "boolean",
          "required": false,
          "doc": "Warning tint."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Destructive tint."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "CodeBlock": {
      "tier": "molecules",
      "props": {
        "code": {
          "type": "string",
          "required": true,
          "doc": "The source text."
        },
        "filename": {
          "type": "string",
          "required": false,
          "doc": "Filename shown in the bar."
        },
        "language": {
          "type": "string",
          "required": false,
          "doc": "The snippet's language: drives the in-kit syntax highlighter (ts/tsx/js/jsx, json, bash, css, html, python; anything else renders monochrome) and the bar label when there is no filename."
        },
        "numbered": {
          "type": "boolean",
          "required": false,
          "doc": "Pinned left gutter of right-aligned, non-selectable line numbers."
        },
        "terminal": {
          "type": "boolean",
          "required": false,
          "doc": "Dark terminal window: chrome bar with traffic lights, prompt-led command lines."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "Tightened type ramp (13/20) and padding for dense contexts."
        },
        "copy": {
          "type": "boolean",
          "required": false,
          "doc": "Opt-in copy affordance, as upstream: a chip (copy glyph + \"Copy\") in the header bar."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Collapsible": {
      "tier": "molecules",
      "props": {
        "label": {
          "type": "React.ReactNode",
          "required": true,
          "doc": "Trigger label."
        },
        "description": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "The muted secondary line under the title in the default trigger anatomy."
        },
        "children": {
          "type": "React.ReactNode",
          "required": true,
          "doc": "Revealed content."
        },
        "defaultOpen": {
          "type": "boolean",
          "required": false,
          "doc": "Start expanded."
        },
        "card": {
          "type": "boolean",
          "required": false,
          "doc": "Card surface: wraps the disclosure in an outlined card container."
        },
        "disabled": {
          "type": "boolean",
          "required": false,
          "doc": "A disabled header does not toggle and reads dimmed."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Command": {
      "tier": "organisms",
      "props": {
        "groups": {
          "type": "Array<{ label: string; items: Array<{ label: string; icon?: string; shortcut?: string }> }>",
          "required": true,
          "doc": "Grouped commands."
        },
        "placeholder": {
          "type": "string",
          "required": false,
          "doc": "Search placeholder."
        },
        "footer": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Footer hint bar below the list."
        },
        "open": {
          "type": "boolean",
          "required": false,
          "doc": "Visibility."
        },
        "onClose": {
          "type": "() => void",
          "required": false,
          "doc": "Dismiss handler."
        },
        "glass": {
          "type": "boolean",
          "required": false,
          "doc": "Glass material."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "DashboardGrid": {
      "tier": "organisms",
      "props": {
        "items": {
          "type": "DashboardWidget[]",
          "required": true
        },
        "columns": {
          "type": "number",
          "required": false,
          "doc": "Track count. Default 12 — it admits sixths, quarters, thirds, halves and a 2:1 split."
        },
        "gap": {
          "type": "number",
          "required": false,
          "doc": "Gutter in px, applied both axes. Default 16."
        },
        "rowHeight": {
          "type": "number",
          "required": false,
          "doc": "Fixed row height in px, making every row uniform. Omit to let rows size to content."
        },
        "storageKey": {
          "type": "string",
          "required": false,
          "doc": "Remember the arrangement across reloads under this key."
        },
        "onReorder": {
          "type": "(order: string[]) => void",
          "required": false,
          "doc": "Fires with the new id order after every move."
        },
        "locked": {
          "type": "boolean",
          "required": false,
          "doc": "Hide the handles and freeze the arrangement."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "DataTable": {
      "tier": "organisms",
      "props": {
        "columns": {
          "type": "DataTableColumn[]",
          "required": true
        },
        "rows": {
          "type": "Array<{ id?: string | number; [key: string]: any }>",
          "required": true,
          "doc": "Row records. Each needs a stable `id` for edit and delete."
        },
        "footer": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Footer content. Pass `footer` bare for the built-in \"n of m rows\" count."
        },
        "sort": {
          "type": "string",
          "required": false,
          "doc": "Controlled sort column. Omit to let the table hold its own."
        },
        "sortDir": {
          "type": "\"asc\" | \"desc\"",
          "required": false,
          "doc": "Controlled sort direction."
        },
        "onSort": {
          "type": "(key: string) => void",
          "required": false,
          "doc": "Sort handler; supply with `sort` to drive sorting yourself."
        },
        "onEdit": {
          "type": "(row: any) => void",
          "required": false,
          "doc": "Adds the trailing edit control; fires when a row opens for editing."
        },
        "onDelete": {
          "type": "(row: any) => void",
          "required": false,
          "doc": "Adds the trailing delete control. Fires after a second confirming click."
        },
        "onSave": {
          "type": "(row: any) => void",
          "required": false,
          "doc": "Fires with the edited row on Save, Enter, or inline-cell blur."
        },
        "inlineEdit": {
          "type": "boolean",
          "required": false,
          "doc": "Click an editable cell straight into a field, no edit button."
        },
        "rowActions": {
          "type": "(row: any) => React.ReactNode",
          "required": false,
          "doc": "Extra per-row controls, placed before edit/delete."
        },
        "onRowClick": {
          "type": "(row: any) => void",
          "required": false,
          "doc": "Row press handler."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "Tighter cell padding."
        },
        "striped": {
          "type": "boolean",
          "required": false,
          "doc": "Tint alternating rows."
        },
        "emptyLabel": {
          "type": "string",
          "required": false,
          "doc": "Empty-state copy."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "DepthChart": {
      "tier": "charts",
      "props": {
        "bids": {
          "type": "Array<DepthLevel | number>",
          "required": false,
          "doc": "Bid side. `{ price, size }`, or bare sizes spaced evenly below `mid`."
        },
        "asks": {
          "type": "Array<DepthLevel | number>",
          "required": false,
          "doc": "Ask side."
        },
        "mid": {
          "type": "number",
          "required": false,
          "doc": "Mid price, pinned at the pane's centre. Defaults to the midpoint of the best bid and ask."
        },
        "title": {
          "type": "string",
          "required": false
        },
        "subtitle": {
          "type": "string",
          "required": false,
          "doc": "Right-aligned caption in the header strip; replaced by the hovered level."
        },
        "height": {
          "type": "number",
          "required": false,
          "doc": "Plot height in px."
        },
        "depthAxis": {
          "type": "boolean",
          "required": false,
          "doc": "Right-hand cumulative-size gutter. Default true."
        },
        "priceAxis": {
          "type": "boolean",
          "required": false,
          "doc": "Price extremes and mid along the floor. Default true."
        },
        "crosshair": {
          "type": "boolean",
          "required": false,
          "doc": "Hover rail and header read-out. Default true."
        },
        "currency": {
          "type": "string",
          "required": false,
          "doc": "Prefix on every price. Default \"$\"."
        },
        "decimals": {
          "type": "number",
          "required": false,
          "doc": "Decimal places on every price. Default 2."
        },
        "compact": {
          "type": "boolean",
          "required": false
        },
        "bare": {
          "type": "boolean",
          "required": false,
          "doc": "Drop the card surface."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "DescriptionList": {
      "tier": "molecules",
      "props": {
        "items": {
          "type": "Array<{ label: string; value: React.ReactNode; mono?: boolean }>",
          "required": true,
          "doc": "The pairs."
        },
        "title": {
          "type": "string",
          "required": false,
          "doc": "Header band title above the rows."
        },
        "subtitle": {
          "type": "string",
          "required": false,
          "doc": "Muted secondary line under the title."
        },
        "layout": {
          "type": "\"twoColumn\" | \"stacked\" | \"inline\"",
          "required": false,
          "doc": "Row arrangement (default \"twoColumn\"): fixed term column, stacked label over value, or inline space-between."
        },
        "divided": {
          "type": "boolean",
          "required": false,
          "doc": "Hairline divider beneath every row but the last."
        },
        "bordered": {
          "type": "boolean",
          "required": false,
          "doc": "Card surface: hairline, card fill, and shadow (default true)."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Dialog": {
      "tier": "organisms",
      "props": {
        "title": {
          "type": "string",
          "required": false,
          "doc": "Dialog title."
        },
        "description": {
          "type": "string",
          "required": false,
          "doc": "Muted supporting line."
        },
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Body content."
        },
        "cancelLabel": {
          "type": "string",
          "required": false,
          "doc": "Cancel action label (default \"Cancel\")."
        },
        "confirmLabel": {
          "type": "string",
          "required": false,
          "doc": "Confirm action label (default \"Confirm\")."
        },
        "onCancel": {
          "type": "() => void",
          "required": false,
          "doc": "Cancel handler (falls back to onClose)."
        },
        "onConfirm": {
          "type": "() => void",
          "required": false,
          "doc": "Confirm handler."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Destructive confirm: web fills the confirm red, iOS keeps the gray capsule and reds only its label, M3 reds the text label."
        },
        "open": {
          "type": "boolean",
          "required": false,
          "doc": "Visibility."
        },
        "onClose": {
          "type": "() => void",
          "required": false,
          "doc": "Backdrop / dismiss handler."
        },
        "glass": {
          "type": "boolean",
          "required": false,
          "doc": "Glass material."
        },
        "size": {
          "type": "\"xs\" | \"small\" | \"medium\" | \"default\" | \"large\" | \"wide\"",
          "required": false,
          "doc": "Size axis: xs 320 / small 384 / medium 448 / (default) 512 / large 576 / wide 672."
        },
        "width": {
          "type": "number",
          "required": false,
          "doc": "Explicit panel width; overrides the size axis."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Divider": {
      "tier": "atoms",
      "props": {
        "vertical": {
          "type": "boolean",
          "required": false,
          "doc": "Vertical rule that stretches to its row."
        },
        "label": {
          "type": "string",
          "required": false,
          "doc": "Centered label between two rules."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Drawer": {
      "tier": "organisms",
      "props": {
        "title": {
          "type": "string",
          "required": false,
          "doc": "Panel title."
        },
        "description": {
          "type": "string",
          "required": false,
          "doc": "Muted line."
        },
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Body."
        },
        "footer": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Footer actions."
        },
        "side": {
          "type": "\"start\" | \"end\" | \"bottom\"",
          "required": false,
          "doc": "Which edge it slides from."
        },
        "open": {
          "type": "boolean",
          "required": false,
          "doc": "Visibility."
        },
        "onClose": {
          "type": "() => void",
          "required": false,
          "doc": "Dismiss handler."
        },
        "glass": {
          "type": "boolean",
          "required": false,
          "doc": "Glass material."
        },
        "size": {
          "type": "number",
          "required": false,
          "doc": "Width, or height when bottom."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Dropdown": {
      "tier": "atoms",
      "props": {
        "trigger": {
          "type": "React.ReactNode",
          "required": true,
          "doc": "The element that opens the menu."
        },
        "label": {
          "type": "string",
          "required": false,
          "doc": "Optional muted section heading rendered above the rows (e.g. \"Actions\")."
        },
        "items": {
          "type": "Array<{ label?: string; icon?: React.ReactNode; shortcut?: string; destructive?: boolean; disabled?: boolean; separator?: boolean; onSelect?: () => void }>",
          "required": true,
          "doc": "Menu rows."
        },
        "open": {
          "type": "boolean",
          "required": false,
          "doc": "Start open."
        },
        "align": {
          "type": "\"start\" | \"end\"",
          "required": false,
          "doc": "Which edge to anchor to."
        },
        "onSelect": {
          "type": "(label: string, item: unknown) => void",
          "required": false,
          "doc": "Selection handler."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Emblem": {
      "tier": "atoms",
      "props": {
        "children": {
          "type": "React.ReactNode",
          "required": true,
          "doc": "The glyph."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "32px tile."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "48px tile."
        },
        "success": {
          "type": "boolean",
          "required": false,
          "doc": "Success tint."
        },
        "warning": {
          "type": "boolean",
          "required": false,
          "doc": "Warning tint."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Destructive tint."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "EmptyState": {
      "tier": "molecules",
      "props": {
        "icon": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Glyph tile."
        },
        "title": {
          "type": "string",
          "required": true,
          "doc": "What is missing."
        },
        "description": {
          "type": "string",
          "required": false,
          "doc": "How to fix it."
        },
        "action": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "The resolving control."
        },
        "bordered": {
          "type": "boolean",
          "required": false,
          "doc": "Hairline card frame (default true)."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "Tighter card padding for dense contexts."
        },
        "success": {
          "type": "boolean",
          "required": false,
          "doc": "Success tone: the disc washes green and tints its glyph."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Feed": {
      "tier": "molecules",
      "props": {
        "items": {
          "type": "Array<{ id?: string; actor?: React.ReactNode; action?: React.ReactNode; text?: React.ReactNode; initials?: string; icon?: React.ReactNode; avatar?: React.ReactNode; time?: string }>",
          "required": true,
          "doc": "The events, oldest or newest first."
        },
        "avatars": {
          "type": "boolean",
          "required": false,
          "doc": "Avatar rows: each row leads with the actor's avatar, separated by hairline rules instead of a connector line."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "Density modifier: tightens the row padding and connector spacing."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Field": {
      "tier": "molecules",
      "props": {
        "label": {
          "type": "string",
          "required": false,
          "doc": "Row label. When the lone child is a field-family control (Input, Select, Autocomplete, Textarea) without a label of its own, the label (and `required`) is DELEGATED into the control so each platform places it per its contract: a static title above the field on web and iOS, the Material 3 in-container floating label on Android. Any other control (a Switch, a group) keeps the static label above."
        },
        "helper": {
          "type": "string",
          "required": false,
          "doc": "Muted hint."
        },
        "error": {
          "type": "string",
          "required": false,
          "doc": "Error text."
        },
        "required": {
          "type": "boolean",
          "required": false,
          "doc": "Mark with an asterisk."
        },
        "children": {
          "type": "React.ReactNode",
          "required": true,
          "doc": "The control."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Gauge": {
      "tier": "charts",
      "props": {
        "value": {
          "type": "number",
          "required": true,
          "doc": "Current value."
        },
        "max": {
          "type": "number",
          "required": false,
          "doc": "Full-scale value (default 100)."
        },
        "label": {
          "type": "string",
          "required": false,
          "doc": "Caption below the readout."
        },
        "size": {
          "type": "number",
          "required": false,
          "doc": "Diameter."
        },
        "success": {
          "type": "boolean",
          "required": false,
          "doc": "Success arc."
        },
        "warning": {
          "type": "boolean",
          "required": false,
          "doc": "Warning arc."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Destructive arc."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "GridList": {
      "tier": "molecules",
      "props": {
        "items": {
          "type": "Array<{ id?: string; title: string; subtitle?: string; media?: React.ReactNode; badge?: React.ReactNode; color?: string }>",
          "required": true,
          "doc": "The records."
        },
        "columns": {
          "type": "2 | 3",
          "required": false,
          "doc": "Tiles per row: 2 or 3 (the default)."
        },
        "gallery": {
          "type": "boolean",
          "required": false,
          "doc": "Gallery mode: each tile is a borderless color-block thumbnail with its meta below."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "Density modifier: tighter gap and tile padding."
        },
        "renderItem": {
          "type": "(item: any, index: number) => React.ReactNode",
          "required": false,
          "doc": "Custom cell renderer."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Heatmap": {
      "tier": "charts",
      "props": {
        "days": {
          "type": "Array<number | { value: number; date?: string }>",
          "required": false,
          "doc": "Chronological days for a CONTRIBUTION grid — 7 weekday rows running left to right by week."
        },
        "startDay": {
          "type": "number",
          "required": false,
          "doc": "Which weekday column the first day lands in (0 = Monday)."
        },
        "rows": {
          "type": "string[]",
          "required": false,
          "doc": "Row labels for the MATRIX shape."
        },
        "columns": {
          "type": "string[]",
          "required": false,
          "doc": "Column labels for the MATRIX shape."
        },
        "values": {
          "type": "number[][]",
          "required": false,
          "doc": "Row-major values for the MATRIX shape."
        },
        "color": {
          "type": "string",
          "required": false,
          "doc": "The hue the five steps are cut from. Default `var(--chart-1)`."
        },
        "title": {
          "type": "string",
          "required": false
        },
        "subtitle": {
          "type": "string",
          "required": false
        },
        "cell": {
          "type": "number",
          "required": false,
          "doc": "Cell edge in px. Defaults to 11 for a contribution grid, 20 for a matrix."
        },
        "gap": {
          "type": "number",
          "required": false,
          "doc": "Gap between cells. Default 3."
        },
        "legend": {
          "type": "boolean",
          "required": false,
          "doc": "Less → More step key. Default true."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Icon": {
      "tier": "atoms",
      "props": {
        "name": {
          "type": "string",
          "required": true,
          "doc": "Lucide kebab-case glyph name, e.g. \"chevron-right\"."
        },
        "size": {
          "type": "number",
          "required": false,
          "doc": "Box size in px (default 16)."
        },
        "muted": {
          "type": "boolean",
          "required": false,
          "doc": "muted-foreground."
        },
        "primary": {
          "type": "boolean",
          "required": false,
          "doc": "Brand."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Destructive."
        },
        "success": {
          "type": "boolean",
          "required": false,
          "doc": "Success."
        },
        "warning": {
          "type": "boolean",
          "required": false,
          "doc": "Warning."
        },
        "color": {
          "type": "string",
          "required": false,
          "doc": "Explicit color override (a token var)."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Input": {
      "tier": "atoms",
      "props": {
        "label": {
          "type": "string",
          "required": false,
          "doc": "Field title, rendered above the control."
        },
        "helper": {
          "type": "string",
          "required": false,
          "doc": "Muted hint below."
        },
        "error": {
          "type": "string",
          "required": false,
          "doc": "Error text; also recolors the border. Always wins over local rules."
        },
        "prefix": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Leading addon box (e.g. \"https://\")."
        },
        "suffix": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Trailing addon box."
        },
        "action": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Trailing action label inside the field."
        },
        "iconLeft": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Overlaid leading glyph."
        },
        "password": {
          "type": "boolean",
          "required": false,
          "doc": "Mask the value and show a reveal toggle in the trailing slot."
        },
        "iconRight": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Overlaid trailing glyph. Overrides the password reveal toggle."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "32px tall."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "40px tall."
        },
        "narrow": {
          "type": "boolean",
          "required": false,
          "doc": "240px field width."
        },
        "wide": {
          "type": "boolean",
          "required": false,
          "doc": "480px field width."
        },
        "block": {
          "type": "boolean",
          "required": false,
          "doc": "Fill the container."
        },
        "placeholder": {
          "type": "string",
          "required": false,
          "doc": "Placeholder text."
        },
        "value": {
          "type": "string",
          "required": false,
          "doc": "Controlled value."
        },
        "defaultValue": {
          "type": "string",
          "required": false,
          "doc": "Initial value."
        },
        "onChange": {
          "type": "(e: React.ChangeEvent<HTMLInputElement>) => void",
          "required": false,
          "doc": "Change handler."
        },
        "disabled": {
          "type": "boolean",
          "required": false,
          "doc": "Dim and disable."
        },
        "type": {
          "type": "string",
          "required": false,
          "doc": "Input type. Also supplies the shape rule for email / url / tel / number."
        },
        "onBlur": {
          "type": "(e: React.FocusEvent<HTMLInputElement>) => void",
          "required": false,
          "doc": "Blur handler."
        },
        "required": {
          "type": "boolean",
          "required": false,
          "doc": "Must not be empty. Also marks the label."
        },
        "minLength": {
          "type": "number",
          "required": false,
          "doc": "Minimum trimmed length."
        },
        "maxLength": {
          "type": "number",
          "required": false,
          "doc": "Maximum trimmed length."
        },
        "pattern": {
          "type": "string",
          "required": false,
          "doc": "Regex source the value must match."
        },
        "min": {
          "type": "number",
          "required": false,
          "doc": "Numeric floor."
        },
        "max": {
          "type": "number",
          "required": false,
          "doc": "Numeric ceiling."
        },
        "validateAs": {
          "type": "\"email\" | \"url\" | \"tel\" | \"number\"",
          "required": false,
          "doc": "Shape rule when `type` can't supply it, e.g. a url in a text field."
        },
        "validate": {
          "type": "(value: string) => string | null | undefined",
          "required": false,
          "doc": "Bespoke rule. Return an error string, or null/undefined when valid."
        },
        "validateOn": {
          "type": "\"blur\" | \"change\"",
          "required": false,
          "doc": "When rules start reporting. Default \"blur\" — then every keystroke once touched."
        },
        "messages": {
          "type": "Partial<Record<\"required\" | \"type\" | \"minLength\" | \"maxLength\" | \"pattern\" | \"min\" | \"max\", string>>",
          "required": false,
          "doc": "Per-rule message overrides, keyed by rule name."
        },
        "submitted": {
          "type": "boolean",
          "required": false,
          "doc": "Force the error to show — set on every field for a form's submit pass."
        },
        "showValid": {
          "type": "boolean",
          "required": false,
          "doc": "Success border and tick once the value passes."
        },
        "onValidate": {
          "type": "(error: string | null) => void",
          "required": false,
          "doc": "Fires with the current error (or null) whenever it changes."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "InputOTP": {
      "tier": "atoms",
      "props": {
        "length": {
          "type": "number",
          "required": false,
          "doc": "How many cells (and the code's length). Default 6."
        },
        "value": {
          "type": "string",
          "required": false,
          "doc": "Controlled code. Omit to let the field own its value."
        },
        "defaultValue": {
          "type": "string",
          "required": false,
          "doc": "Starting code for the uncontrolled field. Default \"\" — it starts EMPTY."
        },
        "active": {
          "type": "number",
          "required": false,
          "doc": "Pin the active ring to one cell for a static specimen. Omit to let the caret drive it."
        },
        "groups": {
          "type": "number",
          "required": false,
          "doc": "Insert a separator every N cells (e.g. 3 for \"123–456\")."
        },
        "numeric": {
          "type": "boolean",
          "required": false,
          "doc": "Digits only, with the numeric keyboard. Default true."
        },
        "autoFocus": {
          "type": "boolean",
          "required": false
        },
        "disabled": {
          "type": "boolean",
          "required": false
        },
        "onChange": {
          "type": "(code: string) => void",
          "required": false,
          "doc": "Fires on every keystroke with the code so far."
        },
        "onComplete": {
          "type": "(code: string) => void",
          "required": false,
          "doc": "Fires once the last cell is filled."
        },
        "small": {
          "type": "boolean",
          "required": false
        },
        "large": {
          "type": "boolean",
          "required": false
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false
        }
      }
    },
    "Kbd": {
      "tier": "atoms",
      "props": {
        "keys": {
          "type": "string | string[]",
          "required": false
        },
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "A single key label, rendered verbatim as exactly ONE cap (never split into a chord)."
        },
        "sequence": {
          "type": "boolean",
          "required": false,
          "doc": "Render `keys` as a sequence (press one, then the next): a wider gap, no \"+\" joiner."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "LineChart": {
      "tier": "charts",
      "props": {
        "series": {
          "type": "LineSeries[]",
          "required": true
        },
        "title": {
          "type": "string",
          "required": false
        },
        "xLabels": {
          "type": "string[]",
          "required": false,
          "doc": "Category labels under the plot."
        },
        "height": {
          "type": "number",
          "required": false,
          "doc": "Plot height in px."
        },
        "dots": {
          "type": "boolean",
          "required": false,
          "doc": "Show a vertex on every point, not just the last and hovered one."
        },
        "smooth": {
          "type": "boolean",
          "required": false,
          "doc": "Curve the line instead of straight segments — for dense, continuous data."
        },
        "area": {
          "type": "boolean",
          "required": false,
          "doc": "Gradient wash under each line."
        },
        "yAxis": {
          "type": "boolean",
          "required": false,
          "doc": "Left value gutter. Default true."
        },
        "yTicks": {
          "type": "number",
          "required": false,
          "doc": "Gridline/tick count. Default 4."
        },
        "crosshair": {
          "type": "boolean",
          "required": false,
          "doc": "Hover crosshair and read-out card. Default true."
        },
        "valueLabels": {
          "type": "boolean",
          "required": false,
          "doc": "Trailing label at each series' end-point. Default true."
        },
        "unit": {
          "type": "string",
          "required": false,
          "doc": "Suffix on every number, e.g. \"%\" or \"k\"."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "Tighter padding and title."
        },
        "bare": {
          "type": "boolean",
          "required": false,
          "doc": "Drop the card surface."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Listbox": {
      "tier": "atoms",
      "props": {
        "options": {
          "type": "Array<{ value: string; label: string; detail?: string } | string>",
          "required": true,
          "doc": "The options."
        },
        "value": {
          "type": "string | string[]",
          "required": false,
          "doc": "Selected value(s)."
        },
        "onChange": {
          "type": "(value: string | string[]) => void",
          "required": false,
          "doc": "Selection handler."
        },
        "multiple": {
          "type": "boolean",
          "required": false,
          "doc": "Allow several selections: rows swap the checkmark gutter for a leading Checkbox control and stay unfilled."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "Tighter rows with smaller text."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "Taller rows."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "MediaObject": {
      "tier": "molecules",
      "props": {
        "media": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Leading avatar, thumbnail, or glyph tile."
        },
        "icon": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "A leading icon glyph rendered in a tinted square box (stands in for an SVG icon)."
        },
        "title": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Primary line."
        },
        "description": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Secondary line."
        },
        "body": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Optional longer body paragraph rendered below the description."
        },
        "meta": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Trailing metadata text pinned to the right (e.g. \"2h ago\", \"admin\")."
        },
        "action": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "A trailing action node (e.g. a <Button>), pinned to the right edge."
        },
        "align": {
          "type": "\"start\" | \"center\"",
          "required": false,
          "doc": "Cross-axis alignment."
        },
        "bordered": {
          "type": "boolean",
          "required": false,
          "doc": "Wraps the row in the card surface (border + padding); omit for a bare row."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "Menu-header density: a 28px leading tile and the title/description type stepped down one size."
        },
        "reversed": {
          "type": "boolean",
          "required": false,
          "doc": "Flips the media to the trailing edge; default leads with the media on the left."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Navbar": {
      "tier": "organisms",
      "props": {
        "brand": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Brand lockup."
        },
        "title": {
          "type": "string",
          "required": false,
          "doc": "Page title."
        },
        "items": {
          "type": "Array<{ label: string; href?: string; active?: boolean }>",
          "required": false,
          "doc": "Inline nav items."
        },
        "actions": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Trailing controls."
        },
        "glass": {
          "type": "boolean",
          "required": false,
          "doc": "Render on the glass functional layer."
        },
        "bordered": {
          "type": "boolean",
          "required": false,
          "doc": "Box the bar on all four sides with a rounded outline."
        },
        "floating": {
          "type": "boolean",
          "required": false,
          "doc": "Lift the bar as a rounded, shadowed card detached from the page edge."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Pagination": {
      "tier": "atoms",
      "props": {
        "page": {
          "type": "number",
          "required": true,
          "doc": "Current page (1-based)."
        },
        "pages": {
          "type": "number",
          "required": true,
          "doc": "Total pages."
        },
        "onChange": {
          "type": "(page: number) => void",
          "required": false,
          "doc": "Page handler."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "32px cells."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "40px cells."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "PieChart": {
      "tier": "charts",
      "props": {
        "data": {
          "type": "PieSlice[]",
          "required": true
        },
        "size": {
          "type": "number",
          "required": false,
          "doc": "Diameter in px. Default 180."
        },
        "donut": {
          "type": "boolean",
          "required": false,
          "doc": "Open the centre for a total read-out."
        },
        "thickness": {
          "type": "number",
          "required": false,
          "doc": "Ring width as a fraction of the radius. Default 0.34."
        },
        "title": {
          "type": "string",
          "required": false
        },
        "subtitle": {
          "type": "string",
          "required": false
        },
        "centerLabel": {
          "type": "string",
          "required": false,
          "doc": "Centre caption when nothing is hovered. Default \"Total\"."
        },
        "centerValue": {
          "type": "string | number",
          "required": false,
          "doc": "Centre value when nothing is hovered. Defaults to the summed total."
        },
        "sliceLabels": {
          "type": "boolean",
          "required": false,
          "doc": "Print each share on its arc. Default true."
        },
        "legend": {
          "type": "boolean",
          "required": false,
          "doc": "Show the legend table. Default true."
        },
        "unit": {
          "type": "string",
          "required": false,
          "doc": "Suffix on every number."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Popover": {
      "tier": "atoms",
      "props": {
        "trigger": {
          "type": "React.ReactNode",
          "required": true,
          "doc": "The anchor element."
        },
        "children": {
          "type": "React.ReactNode",
          "required": true,
          "doc": "Panel content."
        },
        "open": {
          "type": "boolean",
          "required": false,
          "doc": "Controlled open state. Omit to let the popover hold its own."
        },
        "onOpenChange": {
          "type": "(open: boolean) => void",
          "required": false,
          "doc": "Fires whenever the popover opens or closes."
        },
        "width": {
          "type": "number",
          "required": false,
          "doc": "Panel width."
        },
        "glass": {
          "type": "boolean",
          "required": false,
          "doc": "Glass material."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Progress": {
      "tier": "atoms",
      "props": {
        "value": {
          "type": "number",
          "required": true,
          "doc": "0-100."
        },
        "label": {
          "type": "string",
          "required": false,
          "doc": "Leading label."
        },
        "description": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Optional muted secondary line rendered under the title."
        },
        "showValue": {
          "type": "boolean",
          "required": false,
          "doc": "Show the percentage."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "4px track."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "12px track."
        },
        "warning": {
          "type": "boolean",
          "required": false,
          "doc": "Warning fill."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "QRCode": {
      "tier": "atoms",
      "props": {
        "value": {
          "type": "string",
          "required": true,
          "doc": "Encoded payload."
        },
        "size": {
          "type": "number",
          "required": false,
          "doc": "Box size in px."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Radio": {
      "tier": "atoms",
      "props": {
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "The label title."
        },
        "description": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Muted secondary line."
        },
        "checked": {
          "type": "boolean",
          "required": false,
          "doc": "Selected state."
        },
        "onChange": {
          "type": "(checked: boolean) => void",
          "required": false,
          "doc": "Select handler."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "14px ring."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "20px ring."
        },
        "card": {
          "type": "boolean",
          "required": false,
          "doc": "Render as a selectable card row."
        },
        "disabled": {
          "type": "boolean",
          "required": false,
          "doc": "Dim and disable."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Reveal": {
      "tier": "atoms",
      "props": {
        "children": {
          "type": "React.ReactNode",
          "required": true,
          "doc": "Content to reveal."
        },
        "index": {
          "type": "number",
          "required": false,
          "doc": "Ordinal in a group — each step adds 60ms of delay, capped at 10 steps."
        },
        "from": {
          "type": "\"up\" | \"down\" | \"left\" | \"right\" | \"fade\" | \"scale\"",
          "required": false,
          "doc": "Direction the content travels in from. `scale` settles up from 96%; `fade` is opacity only."
        },
        "pronounced": {
          "type": "boolean",
          "required": false,
          "doc": "Travel 40px instead of 16px."
        },
        "brisk": {
          "type": "boolean",
          "required": false,
          "doc": "Run in 260ms instead of 460ms."
        },
        "deep": {
          "type": "boolean",
          "required": false,
          "doc": "Wait until the element is 140px inside the bottom edge instead of 48px."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "ScatterPlot": {
      "tier": "charts",
      "props": {
        "series": {
          "type": "ScatterSeries[]",
          "required": true
        },
        "title": {
          "type": "string",
          "required": false
        },
        "height": {
          "type": "number",
          "required": false,
          "doc": "Plot height in px."
        },
        "xTitle": {
          "type": "string",
          "required": false
        },
        "yTitle": {
          "type": "string",
          "required": false
        },
        "trend": {
          "type": "boolean",
          "required": false,
          "doc": "Dashed least-squares fit across every plotted point."
        },
        "xTicks": {
          "type": "number",
          "required": false
        },
        "yTicks": {
          "type": "number",
          "required": false
        },
        "grid": {
          "type": "boolean",
          "required": false,
          "doc": "Gridlines. Default true."
        },
        "compact": {
          "type": "boolean",
          "required": false
        },
        "bare": {
          "type": "boolean",
          "required": false,
          "doc": "Drop the card surface."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Select": {
      "tier": "atoms",
      "props": {
        "label": {
          "type": "string",
          "required": false,
          "doc": "Field title. Web and iOS render it above the trigger; Android floats the Material 3 in-container label (resting inside the filled box, floating up to body-small once the menu opens or a value is selected)."
        },
        "required": {
          "type": "boolean",
          "required": false,
          "doc": "Marks the label with a destructive asterisk (hidden from assistive tech)."
        },
        "helper": {
          "type": "string",
          "required": false,
          "doc": "Muted hint."
        },
        "options": {
          "type": "Array<{ value: string; label: string } | string>",
          "required": true,
          "doc": "The choices."
        },
        "value": {
          "type": "string",
          "required": false,
          "doc": "Selected value."
        },
        "onChange": {
          "type": "(value: string) => void",
          "required": false,
          "doc": "Selection handler."
        },
        "placeholder": {
          "type": "string",
          "required": false,
          "doc": "Empty-state label."
        },
        "narrow": {
          "type": "boolean",
          "required": false,
          "doc": "240px width."
        },
        "wide": {
          "type": "boolean",
          "required": false,
          "doc": "480px width."
        },
        "block": {
          "type": "boolean",
          "required": false,
          "doc": "Fill the container."
        },
        "disabled": {
          "type": "boolean",
          "required": false,
          "doc": "Dim and disable."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Sidebar": {
      "tier": "organisms",
      "props": {
        "sections": {
          "type": "Array<{",
          "required": true,
          "doc": "Grouped nav items. A section marked `collapsible` becomes an accordion group (pressable header with a rotating per-OS disclosure chevron; one open at a time, and the section owning the active row auto-opens). `defaultOpen` seeds it open; `icon` is its single button in the collapsed rail."
        },
        "active": {
          "type": "string",
          "required": false,
          "doc": "Active item id."
        },
        "onSelect": {
          "type": "(id: string, item: unknown) => void",
          "required": false,
          "doc": "Selection handler."
        },
        "header": {
          "type": "React.ReactNode | ((collapsed: boolean) => React.ReactNode)",
          "required": false,
          "doc": "Brand lockup slot. A render function receives the collapsed state."
        },
        "footer": {
          "type": "React.ReactNode | ((collapsed: boolean) => React.ReactNode)",
          "required": false,
          "doc": "Footer slot, pinned below the rows and KEPT in the collapsed rail. A render function receives the collapsed state so it can show a compact icon-only form."
        },
        "collapsed": {
          "type": "boolean",
          "required": false,
          "doc": "Collapse to the icon rail (controlled)."
        },
        "defaultCollapsed": {
          "type": "boolean",
          "required": false,
          "doc": "Initial collapsed state for uncontrolled use."
        },
        "collapsible": {
          "type": "boolean",
          "required": false,
          "doc": "Show the chevron-left collapse toggle in the header; the collapsed header itself becomes the expand button."
        },
        "onToggleCollapse": {
          "type": "() => void",
          "required": false,
          "doc": "Fired when the collapse/expand toggle is pressed."
        },
        "bordered": {
          "type": "boolean",
          "required": false,
          "doc": "Lift the panel into a fully bordered, rounded card surface."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Skeleton": {
      "tier": "atoms",
      "props": {
        "text": {
          "type": "boolean",
          "required": false,
          "doc": "A 14px text line."
        },
        "button": {
          "type": "boolean",
          "required": false,
          "doc": "A 36px button block."
        },
        "circle": {
          "type": "boolean",
          "required": false,
          "doc": "A circular placeholder."
        },
        "card": {
          "type": "boolean",
          "required": false,
          "doc": "A 64px card surface."
        },
        "width": {
          "type": "number | string",
          "required": false,
          "doc": "Explicit width."
        },
        "height": {
          "type": "number",
          "required": false,
          "doc": "Explicit height."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Slider": {
      "tier": "atoms",
      "props": {
        "value": {
          "type": "number",
          "required": true,
          "doc": "Current value."
        },
        "min": {
          "type": "number",
          "required": false,
          "doc": "Lower bound (default 0)."
        },
        "max": {
          "type": "number",
          "required": false,
          "doc": "Upper bound (default 100)."
        },
        "step": {
          "type": "number",
          "required": false,
          "doc": "Increment."
        },
        "label": {
          "type": "string",
          "required": false,
          "doc": "Leading label."
        },
        "description": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Optional muted secondary line rendered under the title, for a hint or unit note."
        },
        "showValue": {
          "type": "boolean",
          "required": false,
          "doc": "Show the value."
        },
        "onChange": {
          "type": "(value: number) => void",
          "required": false,
          "doc": "Change handler."
        },
        "block": {
          "type": "boolean",
          "required": false,
          "doc": "Fill the container."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "Slimmer track and thumb."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "Thicker track and larger thumb."
        },
        "disabled": {
          "type": "boolean",
          "required": false,
          "doc": "Dim and disable."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Sparkline": {
      "tier": "atoms",
      "props": {
        "data": {
          "type": "number[]",
          "required": true,
          "doc": "The values."
        },
        "width": {
          "type": "number",
          "required": false,
          "doc": "Box width."
        },
        "height": {
          "type": "number",
          "required": false,
          "doc": "Box height."
        },
        "compact": {
          "type": "boolean",
          "required": false,
          "doc": "16px strip."
        },
        "tall": {
          "type": "boolean",
          "required": false,
          "doc": "32px strip."
        },
        "color": {
          "type": "string",
          "required": false,
          "doc": "Line color (a chart token)."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Spinner": {
      "tier": "atoms",
      "props": {
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "14px."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "28px."
        },
        "size": {
          "type": "number",
          "required": false,
          "doc": "Explicit diameter."
        },
        "label": {
          "type": "string",
          "required": false,
          "doc": "Accessible name (default \"Loading\")."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "StackedBar": {
      "tier": "charts",
      "props": {
        "categories": {
          "type": "string[]",
          "required": true
        },
        "series": {
          "type": "BarSeries[]",
          "required": true
        },
        "title": {
          "type": "string",
          "required": false
        },
        "height": {
          "type": "number",
          "required": false,
          "doc": "Plot height in px."
        },
        "grouped": {
          "type": "boolean",
          "required": false,
          "doc": "Segments side by side instead of stacked."
        },
        "stretch": {
          "type": "boolean",
          "required": false,
          "doc": "Normalise every column to 100% for a share-of-mix read."
        },
        "yAxis": {
          "type": "boolean",
          "required": false,
          "doc": "Left value gutter. Default true."
        },
        "yTicks": {
          "type": "number",
          "required": false
        },
        "totals": {
          "type": "boolean",
          "required": false,
          "doc": "Column total above each bar. Default true."
        },
        "unit": {
          "type": "string",
          "required": false,
          "doc": "Suffix on every number."
        },
        "compact": {
          "type": "boolean",
          "required": false
        },
        "bare": {
          "type": "boolean",
          "required": false,
          "doc": "Drop the card surface."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "StackedList": {
      "tier": "molecules",
      "props": {
        "items": {
          "type": "StackedListItem[]",
          "required": true,
          "doc": "The rows."
        },
        "title": {
          "type": "string",
          "required": false,
          "doc": "Header label."
        },
        "headerAction": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Control pinned to the header's trailing edge."
        },
        "divided": {
          "type": "boolean",
          "required": false,
          "doc": "Hairlines between rows (default true)."
        },
        "onItemClick": {
          "type": "(item: StackedListItem) => void",
          "required": false,
          "doc": "Row press handler. Rows only take hover/press feedback when this is set."
        },
        "selectedId": {
          "type": "string | number",
          "required": false,
          "doc": "`id` (or index) of the row to hold selected."
        },
        "reorderable": {
          "type": "boolean",
          "required": false,
          "doc": "Make the rows reorderable: each row gains a leading drag grip and the list becomes a drop target. The order stays CONTROLLED by the consumer's `items` array: a drop only reports the move through `onReorder`, never reorders internally."
        },
        "onReorder": {
          "type": "(move: StackedListMove) => void",
          "required": false,
          "doc": "Fired on drop with the computed move (row id, from/to index, new neighbors)."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Stats": {
      "tier": "molecules",
      "props": {
        "items": {
          "type": "StatItem[]",
          "required": true,
          "doc": "The metrics to render, in order."
        },
        "plain": {
          "type": "boolean",
          "required": false,
          "doc": "Borderless metric stacks on a shared surface, for nesting inside a card."
        },
        "title": {
          "type": "string",
          "required": false,
          "doc": "Optional heading shown above the metrics (mainly for the plain surface)."
        },
        "columns": {
          "type": "number",
          "required": false,
          "doc": "Fixed column count; omit to auto-fit."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Stepper": {
      "tier": "atoms",
      "props": {
        "value": {
          "type": "number",
          "required": true,
          "doc": "Current value."
        },
        "min": {
          "type": "number",
          "required": false,
          "doc": "Lower bound."
        },
        "max": {
          "type": "number",
          "required": false,
          "doc": "Upper bound."
        },
        "step": {
          "type": "number",
          "required": false,
          "doc": "Increment (default 1)."
        },
        "label": {
          "type": "string",
          "required": false,
          "doc": "Field label."
        },
        "description": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Optional muted secondary line rendered under the label."
        },
        "onChange": {
          "type": "(value: number) => void",
          "required": false,
          "doc": "Change handler."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "Narrower buttons and field."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "Wider buttons and field."
        },
        "disabled": {
          "type": "boolean",
          "required": false,
          "doc": "Dim and disable."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Steps": {
      "tier": "organisms",
      "props": {
        "steps": {
          "type": "Array<{ label: string; description?: string } | string>",
          "required": true,
          "doc": "The steps. Each is a label with an optional one-line note."
        },
        "current": {
          "type": "number",
          "required": false,
          "doc": "Index of the active step (controlled)."
        },
        "defaultCurrent": {
          "type": "number",
          "required": false,
          "doc": "Initial active step for uncontrolled use (pressing a step, with `onStepPress`, moves it)."
        },
        "vertical": {
          "type": "boolean",
          "required": false,
          "doc": "Stack vertically."
        },
        "progress": {
          "type": "boolean",
          "required": false,
          "doc": "Render a labeled percentage progress bar instead of discrete steps. Beats `vertical`."
        },
        "value": {
          "type": "number",
          "required": false,
          "doc": "Progress mode only: filled fraction, 0-100 (clamped). Defaults to 0."
        },
        "label": {
          "type": "string",
          "required": false,
          "doc": "Progress mode only: caption shown left of the percentage."
        },
        "onStepPress": {
          "type": "(index: number) => void",
          "required": false,
          "doc": "When set, each step circle is pressable (cursor + press dim), reporting the step index."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Swatch": {
      "tier": "atoms",
      "props": {
        "color": {
          "type": "string",
          "required": true,
          "doc": "The fill: any CSS color string. Pass a theme token, not a literal."
        },
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "The sample's name (the token name). Swatch owns this label column."
        },
        "value": {
          "type": "string",
          "required": false,
          "doc": "Primary mono line under the name: what the name resolves to (a hex, a custom-property name, a ramp)."
        },
        "detail": {
          "type": "string",
          "required": false,
          "doc": "Secondary mono line under the value, for a second notation of the same color (an oklch value, a legacy alias)."
        },
        "small": {
          "type": "boolean",
          "required": false
        },
        "large": {
          "type": "boolean",
          "required": false
        },
        "circle": {
          "type": "boolean",
          "required": false,
          "doc": "Circle instead of the default rounded square. Keeps the 1:1 footprint, so it wins over `block`'s stretch."
        },
        "inline": {
          "type": "boolean",
          "required": false,
          "doc": "Put the label column beside the block instead of under it."
        },
        "block": {
          "type": "boolean",
          "required": false,
          "doc": "Stretch the block to the parent's width; the size then reads as its height (the full-width ramp bar)."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Switch": {
      "tier": "atoms",
      "props": {
        "children": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "The label title, rendered to the left of the trailing track."
        },
        "description": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Muted secondary line."
        },
        "checked": {
          "type": "boolean",
          "required": false,
          "doc": "On state."
        },
        "onChange": {
          "type": "(checked: boolean) => void",
          "required": false,
          "doc": "Toggle handler."
        },
        "disabled": {
          "type": "boolean",
          "required": false,
          "doc": "Dim and disable."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "TabBar": {
      "tier": "organisms",
      "props": {
        "items": {
          "type": "Array<{ key?: string; label: string; icon: string }>",
          "required": true,
          "doc": "The destinations."
        },
        "active": {
          "type": "string",
          "required": false,
          "doc": "Active key."
        },
        "onSelect": {
          "type": "(key: string) => void",
          "required": false,
          "doc": "Selection handler."
        },
        "glass": {
          "type": "boolean",
          "required": false,
          "doc": "Render on the glass functional layer — on iOS this also floats the bar over content."
        },
        "searchKey": {
          "type": "string",
          "required": false,
          "doc": "Key of the search destination. iOS 26 gives it its own trailing capsule."
        },
        "accessory": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "iOS 26 bottom accessory (a mini player, say). Sits above the bar, and beside it once minimized."
        },
        "minimizeBehavior": {
          "type": "\"never\" | \"onScrollDown\" | \"onScrollUp\"",
          "required": false,
          "doc": "When the bar recedes to just the active tab. Default \"never\"."
        },
        "minimized": {
          "type": "boolean",
          "required": false,
          "doc": "Controlled minimized state. Omit to let `minimizeBehavior` drive it."
        },
        "scrollRef": {
          "type": "React.RefObject<HTMLElement>",
          "required": false,
          "doc": "Ref to the scrolling element the bar overlays — required for `minimizeBehavior`."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Tabs": {
      "tier": "organisms",
      "props": {
        "items": {
          "type": "Array<{ value?: string; label: string; count?: number; disabled?: boolean } | string>",
          "required": true,
          "doc": "The tabs."
        },
        "value": {
          "type": "string",
          "required": false,
          "doc": "Active tab."
        },
        "onChange": {
          "type": "(value: string) => void",
          "required": false,
          "doc": "Selection handler."
        },
        "pills": {
          "type": "boolean",
          "required": false,
          "doc": "Muted pill track."
        },
        "vertical": {
          "type": "boolean",
          "required": false,
          "doc": "Vertical rail."
        },
        "block": {
          "type": "boolean",
          "required": false,
          "doc": "Stretch tabs to fill the row."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Textarea": {
      "tier": "atoms",
      "props": {
        "label": {
          "type": "string",
          "required": false,
          "doc": "Field title. Web and iOS render it above the field; Android renders the Material 3 in-container FLOATING label, a multiline float pinned to the first text line at rest, shrinking to body-small above the value once the field is focused or filled."
        },
        "required": {
          "type": "boolean",
          "required": false,
          "doc": "Marks the label with a destructive asterisk (hidden from assistive tech)."
        },
        "helper": {
          "type": "string",
          "required": false,
          "doc": "Muted hint."
        },
        "error": {
          "type": "string",
          "required": false,
          "doc": "Error text."
        },
        "rows": {
          "type": "number",
          "required": false,
          "doc": "Visible rows (default 3)."
        },
        "maxLength": {
          "type": "number",
          "required": false,
          "doc": "Soft cap for the count line: it reads \"N / max\" and turns destructive when over."
        },
        "showCount": {
          "type": "boolean",
          "required": false,
          "doc": "Live character-count line under the field."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "12px field text."
        },
        "large": {
          "type": "boolean",
          "required": false,
          "doc": "16px field text."
        },
        "wide": {
          "type": "boolean",
          "required": false,
          "doc": "480px width."
        },
        "block": {
          "type": "boolean",
          "required": false,
          "doc": "Fill the container."
        },
        "placeholder": {
          "type": "string",
          "required": false,
          "doc": "Placeholder."
        },
        "value": {
          "type": "string",
          "required": false,
          "doc": "Controlled value."
        },
        "defaultValue": {
          "type": "string",
          "required": false,
          "doc": "Initial value."
        },
        "onChange": {
          "type": "(e: React.ChangeEvent<HTMLTextAreaElement>) => void",
          "required": false,
          "doc": "Change handler."
        },
        "disabled": {
          "type": "boolean",
          "required": false,
          "doc": "Dim and disable."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Toast": {
      "tier": "organisms",
      "props": {
        "title": {
          "type": "string",
          "required": true,
          "doc": "Headline. Always the neutral label color; intent never tints it."
        },
        "description": {
          "type": "string",
          "required": false,
          "doc": "Supporting line."
        },
        "action": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Trailing action."
        },
        "icon": {
          "type": "React.ReactNode",
          "required": false,
          "doc": "Override the auto intent glyph; renders on every platform, Android included."
        },
        "onClose": {
          "type": "() => void",
          "required": false,
          "doc": "Show a dismiss control."
        },
        "success": {
          "type": "boolean",
          "required": false,
          "doc": "Green success intent: a tinted circle-check leads."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Red destructive intent: a tinted circle-x leads."
        },
        "warning": {
          "type": "boolean",
          "required": false,
          "doc": "Amber warning intent: a tinted triangle-alert leads."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Tooltip": {
      "tier": "atoms",
      "props": {
        "label": {
          "type": "string",
          "required": true,
          "doc": "The hint."
        },
        "children": {
          "type": "React.ReactNode",
          "required": true,
          "doc": "The target."
        },
        "open": {
          "type": "boolean",
          "required": false,
          "doc": "Force visible."
        },
        "side": {
          "type": "\"top\" | \"bottom\" | \"left\" | \"right\"",
          "required": false,
          "doc": "Which side to show on."
        },
        "reveal": {
          "type": "\"lift\" | \"scale\" | \"fade\" | \"none\"",
          "required": false,
          "doc": "How it enters on hover. Default `lift` — a 4px slide out of the trigger plus a fade."
        },
        "brisk": {
          "type": "boolean",
          "required": false,
          "doc": "Run the reveal in 90ms instead of 140ms."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    },
    "Typography": {
      "tier": "atoms",
      "props": {
        "children": {
          "type": "React.ReactNode",
          "required": true,
          "doc": "The text."
        },
        "display": {
          "type": "boolean",
          "required": false,
          "doc": "48/48 bold display role."
        },
        "h1": {
          "type": "boolean",
          "required": false,
          "doc": "36/40 bold."
        },
        "h2": {
          "type": "boolean",
          "required": false,
          "doc": "30/36 semibold."
        },
        "h3": {
          "type": "boolean",
          "required": false,
          "doc": "24/32 semibold."
        },
        "h4": {
          "type": "boolean",
          "required": false,
          "doc": "20/28 semibold."
        },
        "h5": {
          "type": "boolean",
          "required": false,
          "doc": "18/28 semibold."
        },
        "lead": {
          "type": "boolean",
          "required": false,
          "doc": "16/24 lead paragraph."
        },
        "body": {
          "type": "boolean",
          "required": false,
          "doc": "14/28 reading body."
        },
        "small": {
          "type": "boolean",
          "required": false,
          "doc": "14/20 muted."
        },
        "tiny": {
          "type": "boolean",
          "required": false,
          "doc": "12/16 muted."
        },
        "muted": {
          "type": "boolean",
          "required": false,
          "doc": "Muted tone (or the 14/20 muted role)."
        },
        "caption": {
          "type": "boolean",
          "required": false,
          "doc": "12/16 uppercase, wide tracking."
        },
        "code": {
          "type": "boolean",
          "required": false,
          "doc": "Inline mono pill on the muted fill."
        },
        "mono": {
          "type": "boolean",
          "required": false,
          "doc": "14/20 monospace."
        },
        "primary": {
          "type": "boolean",
          "required": false,
          "doc": "Brand tone."
        },
        "destructive": {
          "type": "boolean",
          "required": false,
          "doc": "Destructive tone."
        },
        "success": {
          "type": "boolean",
          "required": false,
          "doc": "Success tone."
        },
        "warning": {
          "type": "boolean",
          "required": false,
          "doc": "Warning tone."
        },
        "subtle": {
          "type": "boolean",
          "required": false,
          "doc": "60% foreground tone."
        },
        "regular": {
          "type": "boolean",
          "required": false,
          "doc": "400 weight."
        },
        "medium": {
          "type": "boolean",
          "required": false,
          "doc": "500 weight."
        },
        "semibold": {
          "type": "boolean",
          "required": false,
          "doc": "600 weight."
        },
        "bold": {
          "type": "boolean",
          "required": false,
          "doc": "700 weight."
        },
        "tightLeading": {
          "type": "boolean",
          "required": false,
          "doc": "Cap the line box at 1.25x for a stacked lockup."
        },
        "underline": {
          "type": "boolean",
          "required": false,
          "doc": "Underline (an inline text link)."
        },
        "as": {
          "type": "string",
          "required": false,
          "doc": "Override the rendered element."
        },
        "style": {
          "type": "React.CSSProperties",
          "required": false,
          "doc": "Outer layout composition only, never a restyle hook."
        }
      }
    }
  }
}
