---
"@olympusoss/canvas": patch
---

docs: dashboard-grid examples now compose `PageHeader` for the dashboard title + actions row instead of a plain styled paragraph + button — matches the documented pattern for top-of-page chrome and demonstrates intended usage of the molecule. Library code unchanged.
