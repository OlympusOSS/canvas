---
"@olympusoss/canvas": patch
---

`DashboardGrid` edit-mode drag handle now overlays the top-right corner of each widget instead of stealing a 28px-tall strip above it. Frees up vertical space, keeps the card chrome consistent between view and edit modes, and makes the affordance more discoverable next to the widget's own icon.
