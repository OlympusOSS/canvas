---
"@olympusoss/canvas": minor
---

Command: add `revealOnOpen` (search mode). When the palette opens, the search field expands out of its magnifier icon into the full-width bar (a right-anchored width animation, so it grows leftward), then the input and results fade in. Honors Reduce Motion (snaps). Opt in; display-mode usage is unaffected.
