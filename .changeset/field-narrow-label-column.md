---
"@nannier/canvas": patch
---

Field's read-only display mode narrows its label column from 180px to 120px at the sm breakpoint and below, so phone-width label/value rows keep room for the value instead of hard-wrapping it.
