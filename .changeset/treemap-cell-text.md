---
"@olympusoss/canvas": patch
---

Docs: `Treemap` default example now uses a custom cell renderer for cleaner text — white semibold name + smaller muted value, both with a soft drop-shadow for readability on any palette colour. Labels and values automatically hide on cells too small to fit them, so nothing clips on small slices.
