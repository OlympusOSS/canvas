---
"@olympusoss/canvas": patch
---

Docs: `FunnelChart` example switched back to a multi-hue rotation (`chart-1` → `chart-5`) so each stage carries a distinct colour. Reverts the single-hue opacity ramp shipped one patch earlier — multi-colour reads better as the canonical funnel look.
