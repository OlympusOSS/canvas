---
"@nannier/canvas": minor
---

Row and Column gain an `indent` boolean that offsets the whole stack by one control gutter (a control box plus the row gap), so a nested option group lines up under its parent control's label instead of its box (nesting checkboxes or radios under a "select all" parent).
