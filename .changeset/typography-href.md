---
"@nannier-com/canvas": minor
---

`Typography` gains `href`, `hrefAttrs`, and `onPress`: inline text renders as a real browser link on the web (announced as a link; the link role wins over a heading role), native navigates through `onPress`, and `underline` remains the link look. Minor because it is new public Typography API, completing the link affordance `Button` gained for button-shaped navigation.
