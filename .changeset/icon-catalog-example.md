---
"@olympusoss/canvas": patch
---

Docs: Add a "Catalog" example to the `Icon` page that lists every Lucide icon canvas re-exports. Searchable input filters the 1,695-name `iconNames` set live (case-insensitive substring match, deferred so typing stays smooth); rendered grid shows the first 240 matches at any time. Each tile is a button that copies the icon name to clipboard on click — drops the friction of jumping out to the Lucide site to find a name.
