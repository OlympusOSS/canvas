---
"@nannier/canvas": patch
---

Switch's `style` prop is documented as outer layout composition only (never a restyle hook) and widened to `StyleProp<ViewStyle>`, matching the Checkbox and Radio contract.
