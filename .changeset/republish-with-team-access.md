---
"@olympusoss/canvas": patch
---

Republish via CI after granting `@olympusoss:canvas` team `read-write` access to the package and configuring a Granular Access Token with the correct scope. Heals version drift between `package.json` (`2.6.22`) and the npm registry (`2.6.19`) by shipping `2.6.23` through the pipeline.
