---
"@nannier/canvas": patch
---

Route the split ButtonGroup's dropdown menu through AnchoredOverlay so it portals over the page instead of rendering inline, fixing the menu being clipped inside a bounded stage.
