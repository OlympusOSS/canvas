---
"@olympusoss/canvas": patch
---

Docs: `ActivityHeatmap` → `Yearly contribution graph` example uses larger rows (`cellHeight={14}` + `gap={3}`) so the `Mon` / `Wed` / `Fri` row labels have breathing room and don't squish against the cell pitch.
